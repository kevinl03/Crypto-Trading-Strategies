% ======================================================================
%  ABLATION STUDIES
% ======================================================================
\section{Ablation Studies}\label{sec:ablation}

\subsection{Selecting Window Length and Confidence Threshold}

\paragraph{Window $w$.}
Increasing $w$ enriches $(\hat{\mu}_w,\hat{\sigma}_w)$ but forces longer warmup before a valid $z$ exists and slows $\sigma$ adaptation. Offline spreads-only LightGBM sweeps on the Jul~25 holdout show test skill rising into a plateau near $w{\approx}560$--$720$, with only modest gains past the production choice $w{=}300$ (${\approx}{+}1$\,pp DirAcc and ${+}0.017$ R$^2$ at $w{=}720$ for ${\sim}2.4{\times}$ lookback). A $w{\times}\tau$ grid scored on the Jul~31 collector likewise does not favor larger $w$ at matched $\tau$. We therefore retain $w{=}300$.

\paragraph{Threshold $\tau$.}
Early live collection used a permissive gate $\tau{=}0.5$ to maximize coverage under $\text{max\_open}{=}50$. That choice is a starting point for selection, not the paper operating point: on Jul~31 signal rows it yields DirAcc 76.7\%, R$^2$ 0.347, mean z-proxy $+0.75$, and per-trade Sharpe only 0.67 ($n{=}8{,}775$). A fine sweep on the same panel ($w{=}300$) shows that raising $\tau$ monotonically improves DirAcc, R$^2$, mean z-proxy, and per-trade Sharpe $\mathrm{mean}(\mathrm{pnl\_proxy})/\mathrm{std}(\mathrm{pnl\_proxy})$ (Rf~$=0$). \textbf{Per-trade} Sharpe first reaches ${\geq}1$ at $\tau{=}0.9$ (Table~\ref{tab:tau_sensitivity}): DirAcc 84.9\%, R$^2$ 0.514, mean z-proxy $+1.20$, Sharpe 1.02 on $n{=}2{,}026$ rows---still a large book relative to $\tau{=}1.0$ ($n{=}1{,}426$), with mean $\texttt{pnl\_proxy}$ already high versus $\tau{=}0.5$. Neighboring points bound the choice: $\tau{=}0.85$ remains below Sharpe~$1$, while $\tau{=}1.0$ adds little Sharpe for another ${\sim}30\%$ coverage cut. The same $\tau{=}0.9$ cut on Campaign~B closes confirms the lift from the initial $\tau{=}0.5$ book at longer horizon (50{,}690${\to}$12{,}795; DirAcc 79.0\%${\to}$86.7\%; Sharpe/trade 0.75${\to}$1.08). We therefore fix $\tau{=}0.9$ as the paper protocol; headline results use this gate (Section~\ref{sec:limitations} notes when live fills are post-hoc filtered).

\begin{table}[h]
\caption{Choosing $\tau$ from the initial live gate $\tau{=}0.5$ ($w{=}300$). Jul~31 rows are scored signals; Campaign~B rows are live closes. Per-trade Sharpe uses Rf~$=0$.}
\label{tab:tau_sensitivity}
\small
\begin{tabular}{llrrrrr}
\toprule
\textbf{Book} & $\tau$ & $n$ & \textbf{DirAcc} & \textbf{R$^2$} & \textbf{Mean} & \textbf{Sharpe/tr} \\
\midrule
Jul~31 signals & 0.5 & 8{,}775 & 76.7\% & 0.347 & +0.75 & 0.67 \\
Jul~31 signals & 0.85 & 2{,}452 & 84.5\% & 0.497 & +1.16 & 0.99 \\
Jul~31 signals & 0.9 & 2{,}026 & 84.9\% & 0.514 & +1.20 & 1.02 \\
Jul~31 signals & 1.0 & 1{,}426 & 85.3\% & 0.534 & +1.26 & 1.07 \\
$\sim$72h closes & 0.5 & 50{,}690 & 79.0\% & 0.439 & +0.84 & 0.75 \\
$\sim$72h closes & 0.9 & 12{,}795 & 86.7\% & 0.599 & +1.37 & 1.08 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Feature Importance: Role of Microstructure Inputs}

Figure~\ref{fig:importance} shows the top~10 of the model's 68~features by LightGBM split gain.

\begin{figure}[h]
\centering
\includegraphics[width=\columnwidth]{figures/fig4_feature_importance_top20.png}
\caption{Top~10 features by LightGBM split gain. Gain is overwhelmingly
concentrated in lagged z-scores and spread terms; microstructure features
appear only in the lower ranks.}
\Description{Horizontal bar chart of the ten highest-gain features, ordered
from largest at the top. The first bar, lagged z-score at lag one, reaches about
1,660,000 gain. Lagged z-score at lag two follows at about 1,040,000. Spread
in basis points at lag two, coin categorical, spread at lag one, and pair
categorical range from about 310,000 down to 190,000. The remaining four bars
are near-zero against the top features: trade buy-sell ratio lags for Kraken
and Coinbase, cross-venue mid-price range, and Coinbase orderbook imbalance.}
\label{fig:importance}
\end{figure}

The model is, to a first approximation, an autoregression on the recent z-score trajectory conditioned on asset and venue-pair identity, with microstructure features supplying a small correction. Lagged z-scores and spread dynamics dominate the ranking; microstructure and cross-venue dispersion features enter only in the bottom four positions. Only one of the ten highest-gain features is an orderbook imbalance term, and no funding-rate or open-interest feature appears in the top~10.

\subsection{Feature-Group Ablation}

The important predictive block is the \textbf{AR baseline}: lagged z-scores and spreads, short momentum/acceleration, and \texttt{coin}/\texttt{pair} identity. To measure whether microstructure adds anything beyond that, we retrain under a nested leave-one-group-in design on the Jul~25--28 offline holdout with fixed published \texttt{LGBM\_PARAMS} and a shared boosting-round budget (49 rounds). Filtered metrics use the paper gate $|\hat{z}| \geq 0.9$ (Table~\ref{tab:tau_sensitivity}). Funding and open interest prune to 0 columns.

\begin{table}[t]
\caption{Nested feature-group ablation (Jul~25--28 offline, paper gate
$|\hat{z}|\geq 0.9$). $\Delta$R$^2_f$ vs AR baseline; microstructure adds
${\sim}0.01$ R$^2$ or less. Funding/OI pruned.}
\label{tab:logo_ablation}
\footnotesize
\setlength{\tabcolsep}{3.2pt}
\begin{tabular}{@{}lrrrrr@{}}
\toprule
Variant & \# & R$^2$ & R$^2_f$ & DirAcc$_f$ & $\Delta$R$^2_f$ \\
\midrule
AR (lags+mom+id)   & 11 & 0.132 & 0.534 & 85.5\% & --- \\
+ ticker           & 42 & 0.133 & 0.522 & 85.1\% & $-$0.012 \\
+ orderbook        & 45 & 0.132 & 0.519 & 85.2\% & $-$0.015 \\
+ trade flow       & 48 & 0.132 & 0.522 & 85.1\% & $-$0.012 \\
+ funding          & --- & --- & --- & --- & pruned \\
+ open interest    & --- & --- & --- & --- & pruned \\
+ cross / full     & 62 & 0.133 & 0.524 & 85.1\% & $-$0.010 \\
\bottomrule
\end{tabular}
\end{table}

The AR baseline attains the highest filtered R$^2$ (0.534). Adding ticker, orderbook, trade-flow, or cross-venue groups does \emph{not} help: each change is about $0.01$ R$^2$ and slightly negative versus AR. Leave-one-group-out matches this: dropping the baseline collapses filtered R$^2$ from 0.528 to 0.018, while dropping any microstructure family leaves filtered R$^2$ unchanged or slightly higher. The live microstructure panel remains useful as a dataset for other architectures; it is not required for the published LightGBM forecast at $\tau{=}0.9$.
