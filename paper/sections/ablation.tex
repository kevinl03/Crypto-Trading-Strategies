% ======================================================================
%  ABLATION STUDIES
% ======================================================================
\section{Ablation Studies}\label{sec:ablation}

\subsection{Confidence Filter at $\tau=0.5$}

Live campaigns enter when $|\hat{z}| \geq 0.5$---a magnitude gate in rolling-$\sigma$ units (Section~\ref{sec:method}). Table~\ref{tab:filter_ablation} shows that this already-restrictive filter lifts R$^2$ by approximately 0.2--0.25 relative to scoring all predictions, while admitting only 10--15\% of rows (Figure~\ref{fig:filter_lift}). The strategy parallels abstention on low-magnitude forecasts in Sarmento et al.~\cite{sarmento2024ml}.

\begin{table}[h]
\caption{Effect of the $|\hat{z}| \geq 0.5$ confidence filter on model R$^2$ across campaigns.}
\label{tab:filter_ablation}
\small
\begin{tabular}{lrrrr}
\toprule
\textbf{Campaign} & \textbf{All R$^2$} & \textbf{Filtered R$^2$} & \textbf{$\Delta$} & \textbf{\% rows kept} \\
\midrule
Jul~30 (live)  & 0.054 & 0.248 & +0.194 & 10.3\% \\
Jul~31 (live)  & 0.104 & 0.347 & +0.243 & 14.9\% \\
Offline test   & 0.133 & 0.383 & +0.250 & --- \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=\columnwidth]{figures/fig3_filter_r2_lift.png}
\caption{Model R$^2$ before and after the $|\hat{z}| \geq 0.5$ confidence filter. The filter selects predictions in high-signal regimes, lifting R$^2$ by +0.19 to +0.25 across both live campaigns.}
\Description{Grouped bar chart of model R squared on a vertical axis from zero to
0.35 for the two live campaigns. For Jul 30 the all-predictions bar is 0.054 and
the filtered bar is 0.248, a rise of 0.194. For Jul 31 the all-predictions bar is
0.104 and the filtered bar is 0.347, a rise of 0.243. In both campaigns the
filtered bar is roughly three times the height of the unfiltered bar.}
\label{fig:filter_lift}
\end{figure}

A critical caveat at $\tau{=}0.5$: much of the filtered directional accuracy is explained by persistence in high-$|z|$ states. On Jul~31 filtered rows, naive persistence achieves 77.0\% DirAcc versus the model's 76.7\%; the model's advantage is in R$^2$ (0.347 vs 0.174).

\subsection{Selecting $w$ and a Stricter $\tau$}

\paragraph{Window $w$.}
Increasing $w$ enriches $(\hat{\mu}_w,\hat{\sigma}_w)$ but forces longer warmup before a valid $z$ exists and slows $\sigma$ adaptation. Offline spreads-only LightGBM sweeps on the Jul~25 holdout show test skill rising into a plateau near $w{\approx}560$--$720$, with only modest gains past the production choice $w{=}300$ (${\approx}{+}1$\,pp DirAcc and ${+}0.017$ R$^2$ at $w{=}720$ for ${\sim}2.4{\times}$ lookback). A $w{\times}\tau$ grid scored on the Jul~31 collector likewise does not favor larger $w$ at matched $\tau$. We therefore retain $w{=}300$.

\paragraph{Threshold $\tau$.}
The live gate $\tau{=}0.5$ maximizes coverage, not per-trade SNR. A fine sweep on Jul~31 signal rows ($w{=}300$) shows that raising $\tau$ monotonically improves DirAcc, R$^2$, mean z-proxy, and per-trade Sharpe $\mathrm{mean}(\mathrm{pnl})/\mathrm{std}(\mathrm{pnl})$. \textbf{Per-trade} Sharpe first exceeds one at $\tau{=}0.9$ (Table~\ref{tab:tau_sensitivity}): DirAcc 84.9\%, R$^2$ 0.514, Sharpe 1.02 on $n{=}2{,}026$ rows, versus 76.7\% / 0.347 / 0.67 on the $\tau{=}0.5$ set ($n{=}8{,}775$)---about a 77\% reduction in entries. The same post-hoc gate on Campaign~C closes confirms the pattern at longer horizon: 50{,}690${\to}$12{,}795 trades ($-$75\%), DirAcc 79.0\%${\to}$86.7\%, R$^2$ 0.439${\to}$0.599, Sharpe/trade 0.75${\to}$1.08. Fewer trades reduce aggregate z-exposure while raising the PnL-per-trade ratio. We treat $\tau{=}0.9$ as the recommended operating point when prioritizing per-trade forecast quality; primary live tables remain at $\tau{=}0.5$ because that is the policy that was filled under capacity.

\begin{table}[h]
\caption{Stricter confidence gate $|\hat{z}|\geq\tau$ (post-hoc on live books; $w{=}300$). Per-trade Sharpe uses Rf~$=0$.}
\label{tab:tau_sensitivity}
\small
\begin{tabular}{llrrrrr}
\toprule
\textbf{Book} & $\tau$ & $n$ & \textbf{DirAcc} & \textbf{R$^2$} & \textbf{Mean} & \textbf{Sharpe/tr} \\
\midrule
Jul~31 signals & 0.5 & 8{,}775 & 76.7\% & 0.347 & +0.75 & 0.67 \\
Jul~31 signals & 0.9 & 2{,}026 & 84.9\% & 0.514 & +1.20 & 1.02 \\
$\sim$72h closes & 0.5 & 50{,}690 & 79.0\% & 0.439 & +0.84 & 0.75 \\
$\sim$72h closes & 0.9 & 12{,}795 & 86.7\% & 0.599 & +1.37 & 1.08 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Feature Importance: Role of Microstructure Inputs}

Figure~\ref{fig:importance} shows the top~10 of the model's 68~features by LightGBM split gain.

\begin{figure}[h]
\centering
\includegraphics[width=\columnwidth]{figures/fig4_feature_importance_top20.png}
\caption{Top~10 features by LightGBM split gain. Gain is overwhelmingly
concentrated in lagged z-scores and spread terms; microstructure features
appear only in the lower ranks.}
\Description{Horizontal bar chart of the ten highest-gain features, ordered
from largest at the top. The first bar, lagged z-score at lag one, reaches about
1,660,000 gain. Lagged z-score at lag two follows at about 1,040,000. Spread
in basis points at lag two, coin categorical, spread at lag one, and pair
categorical range from about 310,000 down to 190,000. The remaining four bars
are near-zero against the top features: trade buy-sell ratio lags for Kraken
and Coinbase, cross-venue mid-price range, and Coinbase orderbook imbalance.}
\label{fig:importance}
\end{figure}

The model is, to a first approximation, an autoregression on the recent z-score trajectory conditioned on asset and venue-pair identity, with microstructure features supplying a small correction. Lagged z-scores and spread dynamics dominate the ranking; microstructure and cross-venue dispersion features enter only in the bottom four positions. Only one of the ten highest-gain features is an orderbook imbalance term, and no funding-rate or open-interest feature appears in the top~10. The cross-venue dispersion and orderbook features are genuinely unavailable to single-venue or backtest-only systems, but establishing their marginal contribution to R$^2$ requires a leave-one-group-out retraining study that we have not run.
